# Hooks Package (`packages/hooks`)

> Status: **PLACEHOLDER**.

Reserved for shared React hooks that are reused across multiple frontend apps.

## Notes for AI Agents

- Do not add app-specific hooks here.
- Promote hooks into this package only after cross-app reuse is confirmed.
