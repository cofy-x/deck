# Core Python (`packages/core-py`)

> Status: **PLACEHOLDER**.

Shared Python package scaffold for future reusable Python logic.

## Entrypoints

- Package root: `src/core_py`
- Package metadata: `pyproject.toml`

## Development

```bash
uv sync --all-packages
```

## Notes for AI Agents

- Keep this package minimal until Python domain requirements are activated.
