[project]
name = "deck"
version = "0.1.0"
description = "Coordination Fly X Foundation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
managed = true

[tool.uv.workspace]
members = ["apps/server-py", "packages/core-py"]
